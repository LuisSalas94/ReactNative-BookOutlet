import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const EmptyShoppingCart = () => {
  return (
    <View>
      <Text>Your Cart is empty for the moment!</Text>
    </View>
  );
};

export default EmptyShoppingCart;

const styles = StyleSheet.create({});
